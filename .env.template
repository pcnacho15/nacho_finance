DATABASE_URL="postgres://nacho:123456@127.0.0.1:5432/nacho_finance?schema=public"

# Timezone for the Node process + Postgres connection (single-region app).
# Keeps `timestamp` columns as local wall-clock. Defaults to America/Bogota if unset.
TZ="America/Bogota"

# NextAuth (Auth.js v5)
AUTH_SECRET=""
AUTH_TRUST_HOST="true"

# Google OAuth
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Pusher Channels — realtime para el chat. Crea una app en https://dashboard.pusher.com
# Si se dejan vacías, el chat sigue funcionando con polling (sin tiempo real).
PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER=""
# Las dos siguientes deben tener el MISMO valor que PUSHER_KEY / PUSHER_CLUSTER
# (el prefijo NEXT_PUBLIC_ las expone al navegador).
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER=""

# TRON / dApp — Fase 1 (wallet read-only sobre USDT TRC20)
# Desarrollo en Nile testnet. Faucet de USDT/TRX: https://nileex.io/join/getJoinPage
# Para mainnet usar: TRON_FULLHOST=https://api.trongrid.io
#   y TRON_USDT_CONTRACT=TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t
TRON_NETWORK="nile"
TRON_FULLHOST="https://nile.trongrid.io"
# API key gratuita en https://www.trongrid.io (recomendada por rate-limit).
TRONGRID_API_KEY=""
# USDT (TetherToken) en Nile testnet, 6 decimales.
TRON_USDT_CONTRACT="TXYZopYRdj2D9XRtbG411XZZ3kM5VkAeBf"
NEXT_PUBLIC_TRON_NETWORK="nile"
# Mismo valor que TRON_USDT_CONTRACT — el cliente lo usa para construir el envío de USDT.
NEXT_PUBLIC_TRON_USDT_CONTRACT="TXYZopYRdj2D9XRtbG411XZZ3kM5VkAeBf"
# WalletConnect / Reown project id (crea uno gratis en https://cloud.reown.com).
# Vacío = solo TronLink. Con valor, se habilita WalletConnect (wallets móviles por QR).
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""

# EVM / Ethereum (USDT ERC20). Desarrollo en Sepolia; lectura vía RPC público (sin API key).
# Faucet de USDT de prueba (Aave): https://staging.aave.com (o app.aave.com faucet en Sepolia).
# Mainnet: EVM_CHAIN_ID=1, EVM_RPC_URL=https://eth.drpc.org,
#   EVM_USDT_CONTRACT=0xdAC17F958D2ee523a2206206994597C13D831ec7
EVM_NETWORK="sepolia"
EVM_CHAIN_ID="11155111"
EVM_RPC_URL="https://sepolia.drpc.org"
EVM_USDT_CONTRACT="0x7169d38820dfd117c3fa1f22a697dba58d90ba06"
EVM_USDT_DECIMALS="6"
EVM_SYNC_LOOKBACK_BLOCKS="100000"
NEXT_PUBLIC_EVM_NETWORK="sepolia"
NEXT_PUBLIC_EVM_CHAIN_ID="11155111"
NEXT_PUBLIC_EVM_USDT_CONTRACT="0x7169d38820dfd117c3fa1f22a697dba58d90ba06"